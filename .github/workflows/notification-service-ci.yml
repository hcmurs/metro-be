name: notification-ci-flow

on:
  workflow_dispatch:
  pull_request:
    branches: ["main", "main-deployment", "develop"]
    paths:
      - "services/notification-service/**"
      - ".github/workflows/notification-service-ci.yml"
      - "pom.xml"
#   push:
#     branches: ["main", "main-deployment"]
#     paths:
#       - "services/notification-service/**"
#       - ".github/workflows/notification-service-ci.yml"
#       - "pom.xml"

jobs:
  test:
    uses: ./.github/workflows/reusable-service-ci.yml
    with:
      service_name: notification
      service_path: services/notification-service
      run_sonar: false
    secrets: inherit
