name: discovery-ci-flow

on:
  workflow_dispatch:
  pull_request:
    branches: ["main", "main-deployment", "develop"]
    paths:
      - "services/discovery-service/**"
      - ".github/workflows/discovery-service-ci.yml"
      - "pom.xml"
#   push:
#     branches: ["main", "main-deployment"]
#     paths:
#       - "services/discovery-service/**"
#       - ".github/workflows/discovery-service-ci.yml"
#       - "pom.xml"

jobs:
  test:
    uses: ./.github/workflows/reusable-service-ci.yml
    with:
      service_name: discovery
      service_path: services/discovery-service
      run_sonar: false
    secrets: inherit
