spring:
  application:
    name: station-service

  cloud:
    discovery:
      enabled: true

  datasource:
    url: jdbc:postgresql://metro_postgresql:5432/stationdb
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 4004

eureka:
  client:
    serviceUrl:
      defaultZone: http://metro-eureka:8762/eureka/ # Eureka Server URL