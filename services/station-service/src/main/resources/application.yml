spring:
  application:
    name: station-service

  cloud:
    discovery:
      enabled: true

  profiles:
    active: zimaos

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/ # Eureka Server URL
